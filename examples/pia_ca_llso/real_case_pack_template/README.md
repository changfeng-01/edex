# PIA-CA-LLSO Real Case Pack Template

Place externally generated simulator CSV inputs and result directories beside `manifest.yaml`.

Keep the evidence boundary unchanged:

- data_source = real_simulation_csv
- engineering_validity = simulation_only
- must_resimulate = true

This template is for simulation-only validation, not physical validation.
